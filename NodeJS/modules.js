......................Kinds of Modules...................
// Built in modules
const a = require('fs')

/*

	allows to read the file system: example read through the text file and perform operations/ searching and extraction
*/

const b = require('http')
// allows to build the  web server




// npm install nodemon --save-dev
const c = require('nodemon')
// nodemon: detects any changes in our script and displays the changes right away. 
no need of running the script back and forth
similar to npm start: used in react
